
const resolvePalette = (schematic, prefetchedJSONFiles) => 
{

}

const filterPalette = (schematic, prefetchedJSONFiles) => 
{
    
}

const processPalette = (schematic, prefetchedJSONFiles) => 
{
    const filteredPalette = filterPalette(schematic, prefetchedJSONFiles)

}